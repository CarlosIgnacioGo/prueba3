<%= form_for(@song) do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% @song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :duration %><br>
    <%= f.text_field :duration %>
  </div>
  <div>
    <h4>Generos:</h4>
      <ul>
        <% Gender.all.each do |g| %>
          <li>
            <%= f.check_box :gender_id, {multiple: false}, g.id, nil %>
            <span><%=g.name%></span>
          </li>
        <%end%>
        </ul>
      </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
